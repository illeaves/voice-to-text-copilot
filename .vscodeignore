# Development files
.env
.vscode/
.git/
.github/
.gitignore
.gitattributes
.gitmodules
package-lock.json
*.log
*.tmp

# whisper.cpp - ソースコードとドキュメントのみ含める(ビルド生成物は除外)
whisper.cpp/build/
whisper.cpp/models/
whisper.cpp/.git/
whisper.cpp/.github/
whisper.cpp/Testing/

# Node modules - バンドルしない場合は含める必要がある
# (警告が出ているがバンドル化は今回スキップ)

# Keep only whisper-cli binary (main runtime executable)
# All other executables are excluded as they are not needed for the extension

# Runtime generated files
voice.wav
voice.txt
voice_sox.wav
voice_raw.pcm
*.wav
*.mp3
*.m4a
*.ogg

# Build artifacts
*.vsix
out/
dist/

# Documentation (development only)
CHANGELOG.md

# Explicitly include docs folder for README images
!docs/

# Testing
test/
tests/
*.test.js
*.spec.js
coverage/

# IDE specific
.idea/
*.swp
*.swo
*~

# OS specific
.DS_Store
Thumbs.db
desktop.ini

# Temporary files
*.bak
*.backup
temp/
tmp/
